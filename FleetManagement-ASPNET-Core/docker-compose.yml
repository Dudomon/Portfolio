version: '3.8'

services:
  fleetmanagement-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fleetmanagement-api
    ports:
      - "5000:80"
      - "5001:443"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://+:80
      - ConnectionStrings__DefaultConnection=Server=sqlserver;Database=FleetManagementDb;User Id=sa;Password=YourStrong@Passw0rd;TrustServerCertificate=True;
    depends_on:
      - sqlserver
    networks:
      - fleetmanagement-network

  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: fleetmanagement-sqlserver
    environment:
      - ACCEPT_EULA=Y
      - SA_PASSWORD=YourStrong@Passw0rd
      - MSSQL_PID=Developer
    ports:
      - "1433:1433"
    volumes:
      - sqlserver-data:/var/opt/mssql
    networks:
      - fleetmanagement-network

networks:
  fleetmanagement-network:
    driver: bridge

volumes:
  sqlserver-data:
